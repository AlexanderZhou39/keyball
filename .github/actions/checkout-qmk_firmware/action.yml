name: 'Checkout QMK firmware'

inputs:
  version:
    default: '0.22.14'
    type: string
    required: false
  path:
    default: '__qmk__'
    type: string
    required: false

runs:
  using: 'composite'
  steps:

  - name: Checkout qmk_firmware
    uses: actions/checkout@v4
    with:
      path: ${{ inputs.path }}
      repository: vial-kb/vial-qmk
      submodules: recursive
      ref: 'f19fa9854eeb6a59f88d1513f189be4da390d55b'

  - name: Setup QMK
    shell: bash
    run: qmk setup --home ${{ inputs.path }} --yes
